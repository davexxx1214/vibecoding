# 故障排除指南

## 🐛 常见问题和解决方案

### 1. Native 模块版本不兼容

#### 错误信息
```
Failed to activate Knowledge Graph: Error: The module 'better_sqlite3.node'
was compiled against a different Node.js version using
NODE_MODULE_VERSION XXX. This version of Node.js requires
NODE_MODULE_VERSION YYY.
```

#### 原因
`better-sqlite3` 是一个 native 模块（包含 C++ 代码），需要针对 VS Code 的 Electron/Node.js 版本编译。当你的系统 Node.js 版本与 VS Code 内置版本不同时会出现此问题。

#### 解决方案

**方法 1：快速重建（推荐）** ⭐
```bash
npm rebuild better-sqlite3
npm run compile
```

**方法 2：完全重装**
```bash
npm uninstall better-sqlite3
npm install better-sqlite3
npm run compile
```

**方法 3：清理后重装（终极方案）**
```bash
# Windows PowerShell
Remove-Item -Recurse -Force node_modules, package-lock.json
npm install
npm run compile

# Mac/Linux
rm -rf node_modules package-lock.json
npm install
npm run compile
```

---

### 2. 插件没有激活

#### 症状
- 侧边栏没有显示知识图谱图标
- 右键菜单没有 "Knowledge" 选项
- 命令面板找不到 Knowledge 命令

#### 可能原因和解决方案

**原因 1：没有打开工作区文件夹**
- ✅ 必须打开一个文件夹（File > Open Folder）
- ❌ 打开单个文件不会激活插件

**原因 2：插件激活失败**
1. 打开开发者工具：`Help > Toggle Developer Tools`
2. 查看 Console 标签页的错误信息
3. 根据错误信息查找对应解决方案

**原因 3：编译失败**
```bash
npm run compile
# 查看是否有编译错误
```

---

### 3. 数据库初始化失败

#### 错误信息
```
Failed to initialize database: ...
```

#### 可能原因和解决方案

**原因 1：目录权限不足**
- 检查工作区目录是否有写入权限
- 尝试在其他目录创建测试项目

**原因 2：数据库文件损坏**
```bash
# 删除数据库文件重新初始化
# Windows PowerShell
Remove-Item -Recurse -Force .vscode\.knowledge

# Mac/Linux
rm -rf .vscode/.knowledge
```

**原因 3：路径包含特殊字符**
- 确保工作区路径不包含特殊字符（如中文、空格等）
- 尝试使用英文路径

---

### 4. 悬浮提示不显示

#### 可能原因
1. **没有创建实体**
   - 确保已经创建了实体
   - 在侧边栏查看是否有实体

2. **光标位置不正确**
   - 悬浮提示只在实体的代码行范围内显示
   - 确认光标在实体的 startLine 和 endLine 之间

3. **文件路径不匹配**
   - 数据库存储的是相对路径
   - 确保工作区根目录没有变化

---

### 5. CodeLens 不显示

#### 解决方案
1. 检查 VS Code 设置中是否启用了 CodeLens
2. 刷新 CodeLens：
   ```
   Ctrl+Shift+P → "Knowledge: Refresh"
   ```
3. 重新加载窗口：
   ```
   Ctrl+Shift+P → "Reload Window"
   ```

---

### 6. 侧边栏树视图为空

#### 可能原因
1. **还没有创建实体**
   - 创建第一个实体：选中代码 → 右键 → Create Entity

2. **数据库为空**
   - 检查 `.vscode/.knowledge/graph.sqlite` 是否存在
   - 文件大小应该 > 0 KB

3. **树视图需要刷新**
   ```
   Ctrl+Shift+P → "Knowledge: Refresh"
   ```

---

### 7. 编译错误

#### TypeScript 编译错误
```bash
# 检查 TypeScript 版本
npx tsc --version

# 重新安装依赖
rm -rf node_modules
npm install
```

#### esbuild 打包错误
```bash
# 检查 esbuild 配置
cat esbuild.js

# 手动运行 esbuild
node esbuild.js
```

---

### 8. 调试无法启动

#### 检查清单
1. ✅ `.vscode/launch.json` 存在且配置正确
2. ✅ `.vscode/tasks.json` 存在且配置正确
3. ✅ `dist/extension.js` 已生成（运行 `npm run compile`）
4. ✅ 没有其他插件开发窗口正在运行

#### 解决方案
```bash
# 1. 重新编译
npm run compile

# 2. 关闭所有 VS Code 窗口

# 3. 重新打开项目

# 4. 按 F5
```

---

### 9. 性能问题

#### 症状
- 侧边栏加载慢
- 悬浮提示延迟
- 搜索响应慢

#### 优化建议
1. **实体数量过多**
   - 考虑只为重要代码创建实体
   - 按类型或文件分组管理

2. **数据库优化**
   ```sql
   -- 在数据库中运行（使用 SQLite 工具）
   VACUUM;
   ANALYZE;
   ```

3. **清理不需要的实体**
   - 定期清理废弃的实体
   - 删除不再使用的观察记录

---

### 10. Windows 特定问题

#### 路径分隔符问题
- 确保使用正斜杠 `/` 而不是反斜杠 `\`
- 数据库中存储的路径应该是 Unix 风格

#### PowerShell 执行策略
如果无法运行 npm 命令：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

### 11. Mac/Linux 特定问题

#### 权限问题
```bash
# 如果遇到权限错误
sudo chown -R $USER:$USER .
chmod -R 755 .
```

#### Python 依赖（编译 native 模块需要）
```bash
# Mac
xcode-select --install

# Ubuntu/Debian
sudo apt-get install python3 build-essential

# 检查
python3 --version
```

---

## 📊 调试技巧

### 1. 查看日志
在调试窗口中：
```
Help > Toggle Developer Tools > Console
```

### 2. 启用详细日志
在 `src/extension.ts` 中添加更多 `console.log()`

### 3. 检查数据库内容
使用 SQLite 浏览器工具：
- [DB Browser for SQLite](https://sqlitebrowser.org/)
- 打开 `.vscode/.knowledge/graph.sqlite`

### 4. 重新加载插件
在调试窗口中：
- Windows/Linux: `Ctrl+R`
- Mac: `Cmd+R`

---

## 🆘 仍然无法解决？

### 完全重置（最后手段）

```bash
# 1. 删除所有生成的文件
rm -rf node_modules dist .vscode/.knowledge package-lock.json

# 2. 重新安装
npm install

# 3. 重新编译
npm run compile

# 4. 启动调试
# 按 F5
```

### 获取帮助
1. 查看 [QUICKSTART.md](./QUICKSTART.md)
2. 查看 [INSTALL.md](./INSTALL.md)
3. 查看项目 Issues（如果有 Git 仓库）
4. 检查 VS Code 版本（需要 >= 1.80.0）

---

## ✅ 检查列表

遇到问题时，按顺序检查：

- [ ] 1. 是否打开了工作区文件夹？
- [ ] 2. 依赖是否安装？（`npm install`）
- [ ] 3. 代码是否编译？（`npm run compile`）
- [ ] 4. `dist/extension.js` 是否存在？
- [ ] 5. 开发者控制台有无错误？
- [ ] 6. VS Code 版本 >= 1.80.0？
- [ ] 7. Node.js 版本 >= 18.x？
- [ ] 8. better-sqlite3 是否正确编译？（`npm rebuild better-sqlite3`）

---

**最后更新**: 2025-11-04  
**版本**: 0.1.0

