# 调试指南

## 🔍 如何查看插件运行日志

### 打开开发者控制台

在 **Extension Development Host**（调试窗口）中：

**方法 1：使用菜单**
```
Help → Toggle Developer Tools
```

**方法 2：使用快捷键**
- Windows/Linux: `Ctrl+Shift+I`
- Mac: `Cmd+Option+I`

### 查看日志

1. 点击 **Console** 标签页
2. 查看插件的日志输出

### 正常启动日志

如果插件正常启动，你应该看到：

```
Knowledge Graph extension is now active
Workspace root: D:\workspace\your-project
Initializing database...
Database initialized successfully
Registering commands...
All commands registered successfully
✅ Knowledge Graph extension activated successfully!
```

### 命令执行日志

当你执行命令时，会看到：

```
Executing: knowledge.createEntity
```

或

```
Executing: knowledge.addObservation
```

## 🐛 常见问题诊断

### 1. 命令点击无反应

#### 检查项 1：是否打开了工作区文件夹？

❌ **错误情况**：打开单个文件
```
Knowledge Graph: Please open a folder to use this extension
```

✅ **正确做法**：
1. File → Open Folder
2. 选择一个项目文件夹
3. 重新加载插件（Ctrl+R / Cmd+R）

#### 检查项 2：查看控制台错误

如果控制台显示错误，例如：
```
Error in createEntity: ...
```

记下错误信息，查看 [故障排除.md](./故障排除.md)

#### 检查项 3：命令是否注册成功？

在命令面板（Ctrl+Shift+P）中输入 "Knowledge"，应该能看到所有命令：
- Knowledge: Create Entity from Selection
- Knowledge: Add Observation to Entity...
- Knowledge: View Entity Details
- Knowledge: Search Graph
- 等等...

如果看不到这些命令，说明插件没有正确激活。

### 2. 数据库初始化失败

#### 错误日志示例
```
Failed to activate Knowledge Graph: Error: EACCES: permission denied
```

#### 解决方案
1. 检查工作区目录权限
2. 查看 `.vscode/.knowledge/` 目录是否可以创建
3. 尝试在其他目录创建测试项目

### 3. 选中代码后右键无 Knowledge 选项

#### 可能原因
1. **插件没有激活**
   - 查看控制台日志
   - 确认看到 "activated successfully" 消息

2. **菜单注册失败**
   - 重新加载窗口：Ctrl+R / Cmd+R
   - 关闭调试窗口，重新按 F5

3. **package.json 配置错误**
   - 检查 `contributes.menus` 部分
   - 重新编译：`npm run compile`

## 🔬 高级调试技巧

### 1. 在代码中设置断点

1. 在 VS Code 主窗口中打开 `src/extension.ts`
2. 点击行号左侧设置断点（红点）
3. 在调试窗口中执行命令
4. 程序会在断点处暂停，可以查看变量值

### 2. 添加更多日志

在需要调试的地方添加 `console.log()`：

```typescript
console.log('当前变量值:', someVariable);
console.log('执行到这里了');
```

然后：
```bash
npm run compile
# 在调试窗口按 Ctrl+R / Cmd+R
```

### 3. 查看数据库内容

使用 [DB Browser for SQLite](https://sqlitebrowser.org/) 打开：
```
<工作区>/.vscode/.knowledge/graph.sqlite
```

可以查看：
- entities 表：所有实体
- relations 表：所有关系
- observations 表：所有观察记录

### 4. 清理并重新开始

如果问题无法解决，尝试完全重置：

```bash
# 1. 关闭所有 VS Code 窗口

# 2. 删除测试数据
rm -rf <测试项目>/.vscode/.knowledge

# 3. 重新编译
cd <插件项目>
npm run compile

# 4. 按 F5 重新启动调试
```

## 📋 调试检查清单

遇到问题时，按顺序检查：

- [ ] 1. 是否打开了工作区文件夹？
- [ ] 2. 开发者控制台是否有错误？
- [ ] 3. 插件是否显示 "activated successfully"？
- [ ] 4. 命令面板能否找到 Knowledge 命令？
- [ ] 5. 右键菜单是否有 Knowledge 选项？
- [ ] 6. 点击命令时控制台是否有 "Executing:" 日志？
- [ ] 7. 是否有具体的错误消息？

## 🎯 快速测试流程

### 完整测试步骤

1. **启动调试**
   ```
   在插件项目中按 F5
   ```

2. **打开测试项目**
   ```
   File → Open Folder → 选择任意项目
   ```

3. **查看激活状态**
   ```
   应该看到通知："✅ Knowledge Graph extension activated successfully!"
   ```

4. **打开开发者控制台**
   ```
   Help → Toggle Developer Tools
   ```

5. **创建测试文件**
   ```typescript
   // test.ts
   function hello() {
     console.log('Hello');
   }
   ```

6. **创建实体**
   ```
   1. 选中 "function hello"
   2. 右键 → Knowledge: Create Entity from Selection
   3. 查看控制台输出
   ```

7. **预期结果**
   - 控制台显示：`Executing: knowledge.createEntity`
   - 弹出输入框：要求输入实体名称
   - 输入名称后，选择类型
   - 显示成功消息："Entity created successfully"

## 💡 调试提示

### 提示 1：保持控制台打开
在调试期间，始终保持开发者控制台打开，可以实时看到日志。

### 提示 2：使用过滤器
在控制台中，可以过滤日志：
```
在 Filter 输入框中输入：Knowledge
```
只显示包含 "Knowledge" 的日志。

### 提示 3：清空控制台
每次重新测试前，点击 🚫 图标清空控制台，更容易看清新的日志。

### 提示 4：复制错误信息
如果遇到错误，右键点击错误消息 → Copy → 粘贴到文档中分析。

## 📞 获取帮助

如果按照以上步骤仍无法解决问题：

1. 复制完整的控制台输出
2. 说明你的操作步骤
3. 提供错误截图
4. 查看 [故障排除.md](./故障排除.md)

---

**记住**：调试是开发的正常过程，不要着急，逐步排查问题！

最后更新：2025-11-04

